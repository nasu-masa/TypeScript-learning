{
  //2. 配列の型

  // 配列の型を定義するには、2つの書き方がある。

  const numbers: number[] = [1, 2, 3, 4, 5];
  const names: string[] = ["Taro", "Hanako", "Jiro"];

  const scores: Array<number> = [80, 90, 75];

  const mans: string[] = ["Soichiro", "Kojiro", "Kanzaburo"];

  const fruits: Array<string> = ["banana", "apple", "grape"];

  console.log(mans);
  console.log(fruits);
}
